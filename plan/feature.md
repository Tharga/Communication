# Feature: icon-and-docs

## Request
Two meta-repo-wide initiatives in central Requests.md (both Pending for Tharga.Communication, both 2026-06-01):
- **Move PackageIconUrl to thargelion.net/assets** — replace WordPress URLs with `https://thargelion.net/assets/component-<name>.png`
- **Documentation sites under tharga.net** — DocFX site at `communication.tharga.net`, GitHub Pages deploy

Bundled into one feature/PR matching the precedent set by `Tharga.Mcp` and `Tharga.Runtime` (both shipped on branch `feature/icon-and-docs` in a single PR).

## Goal
1. Update both `Tharga.Communication.csproj` and `Tharga.Communication.Mcp.csproj` to use `https://thargelion.net/assets/component-communication.png` (one icon for both packages — matches Mcp/Runtime pattern of one icon per repo).
2. Add a `docs/` source tree using DocFX (same structure as Tharga.Mcp).
3. Add `docs` + `docs-deploy` jobs to `.github/workflows/build.yml` (depend on `release`, run only on push to master).
4. Configure `docs/CNAME = communication.tharga.net`.
5. Update root `README.md` to link to the docs site.

## Scope

### Icon URL migration
- `Tharga.Communication/Tharga.Communication.csproj` → `<PackageIconUrl>https://thargelion.net/assets/component-communication.png</PackageIconUrl>`
- `Tharga.Communication.Mcp/Tharga.Communication.Mcp.csproj` → same (currently uses the WordPress URL too)
- Verify URL resolves before committing

### Docs site (`communication.tharga.net`)
Mirror Tharga.Mcp's docs structure exactly:

```
docs/
├── CNAME                          (single line: communication.tharga.net)
├── docfx.json                     (with _appLogoPath / _appFaviconPath pointing at thargelion.net/assets/component-communication.png)
├── index.md                       (landing page)
├── toc.yml                        (Home / Articles / API)
├── articles/
│   ├── index.md                   (article index)
│   ├── getting-started.md
│   ├── messaging.md               (PostAsync, SendMessage)
│   ├── subscriptions.md           (SubscribeAsync, HasSubscribers, PostIfSubscribedAsync)
│   ├── authentication.md          (IApiKeyValidator, ApiKeys, custom validator + IHttpContextAccessor hint)
│   └── toc.yml
├── api/                           (generated by DocFX)
└── templates/thg/public/main.css  (logo size constraint, copied from Mcp)
```

The four articles align with the existing README sections (getting-started + the three feature areas).

### GHA workflow additions
Append two jobs to `.github/workflows/build.yml`, copy-pasted from the Mcp build.yml pattern:

```yaml
docs:
  needs: release
  runs-on: ubuntu-latest
  if: github.ref == 'refs/heads/master' && github.event_name == 'push'
  steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-dotnet@v4 (with 8/9/10)
    - run: dotnet tool install -g docfx
    - run: dotnet restore
    - run: docfx docs/docfx.json
    - uses: actions/upload-pages-artifact@v3
      with: { path: docs/_site }

docs-deploy:
  needs: docs
  runs-on: ubuntu-latest
  if: github.ref == 'refs/heads/master' && github.event_name == 'push'
  concurrency: { group: pages, cancel-in-progress: false }
  environment: { name: github-pages, url: ${{ steps.deployment.outputs.page_url }} }
  steps:
    - uses: actions/deploy-pages@v4
```

Workflow already has `pages: write` permission (verify).

### README
Add a "Docs" line at the top, like Tharga.Mcp's `**Docs:** [mcp.tharga.net](https://mcp.tharga.net) — guides, API reference, and the provider/scope/auth walkthroughs.`

## Out of scope
- No code changes — purely docs/metadata
- No version bump beyond patch (this is metadata; next CI tag will be the next patch in 0.2.x)
- `PackageProjectUrl` stays on GitHub (matches Mcp/Runtime — source-first parity with Tharga.MongoDB)

## Pre-requisites (meta-repo, assumed done)
- `component-communication.png` already uploaded to `https://thargelion.net/assets/` — verify before committing csproj change
- DNS `communication.tharga.net → tharga.github.io` configured — verify after first deploy
- GitHub Pages enabled for the repo (Settings → Pages, source: GitHub Actions)

## Acceptance criteria
- [ ] Icon URL updated in both csproj files
- [ ] `https://thargelion.net/assets/component-communication.png` resolves (HTTP 200)
- [ ] `docs/` tree builds locally via `docfx docs/docfx.json` with no errors
- [ ] All 4 articles render
- [ ] API reference generates from both `Tharga.Communication.csproj` and `Tharga.Communication.Mcp.csproj` (or at least the main lib — Mcp/Runtime only metadata-process one csproj)
- [ ] CNAME set to `communication.tharga.net`
- [ ] Workflow `docs` + `docs-deploy` jobs added
- [ ] README links to the docs site
- [ ] PR merges and the GHA pipeline publishes the site
- [ ] DNS resolves and the site loads at `https://communication.tharga.net`
- [ ] Both Requests.md entries marked Done

## Done condition
PR merged, site live at `communication.tharga.net`, next package release includes the new `PackageIconUrl`, both central Requests.md items updated.
